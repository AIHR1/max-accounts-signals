# Procedure SOPs

This folder holds **only** reusable procedure notes: `**sop-master-digest`** (orchestration) and `**sop-1-…` through `sop-6-…**` (signal definitions). The account table is `**../accounts-in-scope.md**`. Each execution’s digest, manifest, and logs live under `**../runs/{run-id}/**` — see `**../runs/README.md**`.

## See also

- [[sop-master-digest]] — Master orchestration SOP
- [[accounts-in-scope]] — Account scope definition